fastapi>=0.115.0
# multipart 表单(UploadFile/Form 解析,skills zip 上传用)
python-multipart>=0.0.9
uvicorn[standard]>=0.30.6
sqlalchemy>=2.0.35
alembic>=1.13.3
# 用 psycopg3 替代 psycopg2-binary,纯 Python 无需 pg_config,Python 3.14 兼容
psycopg[binary]>=3.2.0
pydantic>=2.9.2
pydantic-settings>=2.5.2
python-dotenv>=1.0.1
# 阶段 6:认证授权
# bcrypt:密码加盐哈希(security.py)
bcrypt>=4.2.0
# PyJWT:JWT 签发与校验(security.py,import 时为 import jwt)
PyJWT>=2.9.0
# email-validator:Pydantic EmailStr 字段校验(schemas/user.py)
email-validator>=2.2.0
# httpx:GitHub OAuth 调用(github_oauth.py)
httpx>=0.27.2
# cryptography:GitHub access_token 加密存储(app/security.py Fernet)
cryptography>=43.0.0
# LLM
openai>=1.51.0
# 沙箱(阶段 2 起,Server 部署在 Linux 上)
# 若本地未部署 OpenSandbox Server,可设 SANDBOX_MODE=mock 跳过实际调用
opensandbox>=0.1.0

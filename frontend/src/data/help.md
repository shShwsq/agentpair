# AgentPair 使用帮助

AgentPair 是一个**双智能体协作系统**:用户描述任务后,两个智能体分工协作完成。

## 核心概念

### user_agent(评估智能体)

负责**澄清意图**和**评估结果**:

- 任务开始时,若你的描述不够具体,它会通过弹窗向你提问(最多 2 轮)
- 任务开始时会推送一份动态生成的覆盖度清单,你可编辑后确认
- react_agent 提交结果后,它对照清单评估是否完整
- 若发现未覆盖项,它会打断 react_agent 让其补充
- (实验性)可在协作策略中开启「自行验证」,让它在沙箱里跑 PoC / HTTP 请求动态验证发现

### react_agent(执行智能体)

负责**调用工具执行任务**:

- 内置 react_agent:直接用上方选择的 LLM 模型
- 外部 CLI agent:已配置且启用的外部 CLI(Qoder / Kimi / Hermes / Codex),在提交任务页可切换

两者通过**检查点评估**衔接:react_agent 每完成一轮,user_agent 评估一次,决定继续、追问还是完成。

### 验证动作(实验性)

开启「自行验证」后,user_agent 会在沙箱内调用 `http_request` / `run_python_code` 动态验证 react_agent 的发现(如确认 SQL 注入是否真实可利用)。验证动作有两种授权模式:

- `per_action`:每个动作执行前弹窗让你确认(显示完整 URL / 头 / 请求体或代码)
- `direct`:不弹窗,直接执行

可在「协作策略」页设默认模式,任务创建时可覆盖。

## 页面导览

### 首页 `/`

欢迎落地页。点"提交新任务"进入任务提交页。左上角按钮可展开历史任务侧栏。

### 提交任务 `/tasks/new`

1. **选择场景**:场景提供预设 prompt 模板,选择后自动填入输入框;也可自由输入覆盖
2. **选择 user_agent 模型**:用于结果评估。若列表为空,先到「模型设置」配置 LLM 凭据
3. **选择 react_agent 执行器**:内置 react_agent 直接用上面的模型(也可选另一个模型作 react_agent 模型);若已配置外部 CLI 可在此切换
4. **填写任务描述**:尽量具体——目标、范围、约束、期望产物格式
5. **填写仓库地址与分支**:react_agent 会克隆该仓库到沙盒中执行
6. **点发送按钮**:后端立即返回 task_id 并跳转到任务详情页观看实时进度

### 任务详情 `/tasks/:id`

实时观看任务执行:

- **结果清单**:任务产出的结果按维度分组(安全审计按严重程度,代码审查按类别)展示
- **协作对话流**:user_agent 与 react_agent 的对话过程,包含思考、工具调用、检查点评估、计划清单
- **暂停 / 恢复**:运行中可随时暂停,补充信息后恢复执行;暂停期间可向智能体追加指令
- **历史任务切换**:左上角按钮展开侧栏,在多个任务之间快速切换

### 模型设置 `/models`

配置 LLM 凭据(API Key、Base URL、模型名)。支持多个凭据,提交任务时选用。

### CLI 设置 `/cli`

配置外部 CLI 凭据(如 Qoder / Kimi / Hermes / Codex 的 API Key 或 PAT)。配置并启用后,在提交任务页可切换执行器。

### 协作策略 `/agent-policy`

调整 user_agent 的协作行为(用户级默认,任务级可覆盖):

- **启用 user_agent**:关闭后退化为单 agent 模式(react_agent 跑 1 轮即输出)
- **协作总轮次**:1-10,默认 4
- **评估频率 K**:user_agent 每 K 个 react_agent 迭代做一次轻量检查点评估
- **允许打断**:是否允许 user_agent 中断 react_agent 追问
- **每轮最大打断**:防死锁上限,默认 2
- **自行验证(实验性)**:允许 user_agent 跑 PoC / HTTP 请求动态验证发现
- **验证授权模式**:`per_action`(每动作弹窗确认)/ `direct`(直接执行)

### 技能管理 `/skills`

上传并管理自定义技能(react_agent 可调用的多步操作模板):

- 上传 zip 格式的 skill(内含 `SKILL.md`,支持 `<skill_name>/SKILL.md` 或根目录直接放)
- 上传后 react_agent 的 `list_skills` / `skill` 工具立即可用
- 技能仅自己可见,他人无法查看或使用
- 系统内置 skill(如 `check_sql_injection`、`check_hardcoded_secrets`、`check_ssrf`)只读,不能删除
- 自己上传的 skill 可在线编辑 `SKILL.md` 并热保存

### 记忆管理 `/memory`

以「文件」隐喻管理三类长期记忆,均为 Markdown 自由文本,注入到对应智能体的系统提示词:

- **用户偏好**(1 份,上限 2000 字符):注入 user_agent,影响评估倾向与判据
- **全局记忆**(1 份,上限 20000 字符):跨项目通用经验,同时注入 user_agent 与 react_agent
- **项目记忆**(N 份,按 `repo_url` 聚合,上限 20000 字符):任务完成后由 LLM 自动归纳生成,注入 react_agent;可编辑项目别名便于识别

预览用 `marked` 渲染 + `DOMPurify` 净化(防存储型 XSS)。Agent 协作策略已迁至 `/agent-policy`,本页只管记忆文本。

### 账号设置 `/settings`

(由顶栏齿轮按钮进入,不在主导航)

- 修改密码(需验证当前密码)
- GitHub / Gitee 账号绑定(用于私有仓库 clone)
- 登出设备管理
- 删除账号(需输入完整邮箱二次确认,硬删除级联清理所有数据)

## 顶栏辅助按钮

右侧依次为:

- **问号**:打开本帮助文档弹窗
- **主题切换**:浅色 / 深色 / 跟随系统(选择持久化到 localStorage)
- **齿轮**:进入账号设置
- **登出**

## 任务运行中的弹窗

任务运行过程中可能弹出以下对话框:

### 澄清提问(QuestionDialog)

user_agent 第 0 轮初始评估时,若认为你的意图不够清晰,会弹窗提问。支持选择题(单选 / 多选)和填空题。最后一题固定为「是否有其他补充」。最多提问 2 轮,超过后强制开始执行。

### 覆盖度清单确认(ChecklistReviewDialog)

user_agent 第 0 轮根据你的任务说明动态生成覆盖度清单(3-8 个维度,每个含若干检查项)。弹窗让你编辑确认:

- 可改维度名称、描述、增删检查项、增删维度
- 可「直接采用」智能体生成的清单不做修改
- 确认后落库,后续协作轮按此清单评估

### 验证动作授权(VerifyActionDialog)

开启「自行验证」且授权模式为 `per_action` 时,user_agent 每次执行 `http_request` 或 `run_python_code` 前会弹窗让你确认。弹窗显示完整请求(URL / 方法 / 头 / 体)或代码内容,你可选「拒绝」或「同意执行」。

### 危险命令确认(CommandConfirmDialog)

危险命令确认有两种场景,共用同一个弹窗组件:

**1. local 模式(无沙箱,`SANDBOX_MODE=local`)**

智能体调用 `run_command` 执行的危险命令(如 `rm -rf`、`curl | sh`、`sudo`、`mkfs` 等)会弹窗让你确认。命令在宿主机上直接执行,请确认内容无害后再同意。安全命令(`git status` / `ls` / `cat` 等)直接执行不拦截。

**2. CLI 执行智能体(`per_command` 模式)**

选择 qoder / kimi / hermes / codex 作为执行器且命令确认模式设为「逐命令确认」时,CLI 执行危险命令前会通过 ACP 协议发送 `request_permission` 请求,前端弹窗显示命令内容让你确认。可在「协作策略」页设默认模式,新建任务时也可单独覆盖。

- **自动批准**(默认):注入 YOLO 模式,所有命令直接执行不弹窗(速度快,适合可信任务)
- **逐命令确认**:每个危险命令弹窗确认(更安全,防容器破坏 / 资源耗尽)
- **Codex 限制**:`codex exec --json` 是非交互模式,不支持逐命令确认,选 `per_command` 时会自动降级为自动批准并警告。如需命令确认,请改用 qoder / kimi / hermes。

**3. 内置 react_agent(`per_command` 模式,sandbox 模式下)**

选择「内置」执行器且命令确认模式设为「逐命令确认」时,内置 react_agent 执行危险命令前会推 `command_confirm` SSE 事件,前端弹窗显示命令内容让你确认。与 local 模式复用同一套机制(`_PendingCommandConfirm`)。

- **自动批准**(默认):sandbox 模式下危险命令直接执行(沙箱已隔离,破坏范围限于容器内)
- **逐命令确认**:sandbox 模式下危险命令弹窗确认(更安全,防容器破坏 / 资源耗尽)
- **local 模式特例**:无论选哪个模式,local 模式下 dangerous 命令始终推确认(宿主机直接执行,无法绕过)

## 快捷操作

### 任务详情页键盘快捷键

- `Enter` 在输入框中发送补充指令
- `Shift + Enter` 换行

### 新手引导

首次进入首页 / 提交任务页 / 任务详情页时会自动弹出新手指引气泡。可点击"跳过引导"关闭,完成后不再自动弹出。引导文案有大改时会递增版本号,老用户会重新看到引导。

> 若想重新查看新手引导:目前未提供入口。如需重看,清浏览器 localStorage 中 `agentpair:onboarding:completed:*` 相关 key 即可。

## 常见问题

### 提交任务后没反应?

- 检查浏览器控制台是否有 SSE 连接错误
- 确认模型凭据有效(API Key 未过期)
- 仓库地址是否可访问(私有仓库需先绑定 GitHub / Gitee)

### react_agent 报错"模型未配置"?

到「模型设置」配置至少一个 LLM 凭据,提交任务时选择该模型。

### user_agent 一直追问?

它的追问是为了澄清意图,确保任务完整。如实回答即可。若问题与任务无关,可在回答中说明。

### 任务被暂停了?

- 可能是 user_agent 主动打断(发现需要用户澄清)
- 也可能是手动点了暂停按钮
- 在暂停状态下输入补充信息后点"恢复"即可继续

### 想更换执行器但下拉框没有选项?

到「CLI 设置」配置外部 CLI 凭据并启用,回到提交任务页即可看到新选项。

### 想让 react_agent 自动按固定步骤执行?

到「技能管理」上传或使用内置技能(如 `check_sql_injection`),react_agent 会在合适时机主动调用。

### 验证动作弹窗太频繁?

到「协作策略」把「验证授权模式」改为 `direct`,验证动作将不再弹窗直接执行(注意:这会放行所有 PoC / HTTP 请求,请确认测试环境隔离)。

### local 模式下危险命令弹窗太多?

local 模式下,智能体的命令在宿主机直接执行,危险命令会弹窗确认。建议切到 `SANDBOX_MODE=sandbox` 用沙箱隔离,就不会有这些弹窗。生产环境务必用 sandbox 模式。

### CLI 执行智能体命令弹窗太多?

到「协作策略」把「CLI 命令确认模式」改为「自动批准」,CLI 执行危险命令时将不再弹窗直接执行(注入 YOLO 模式)。注意:这会放行所有危险命令,请确认任务可信且沙箱隔离到位。新建任务时也可单独设置覆盖协作策略页的默认值。

---

如有其他问题,请提交 GitHub Issue 或联系系统管理员。
